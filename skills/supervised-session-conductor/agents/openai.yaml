interface:
  display_name: "Supervised Session Conductor"
  short_description: "여러 작업자의 소유권·보고·완료 신호를 한 게이트로 조정합니다"
  default_prompt: "Use $supervised-session-conductor to reconcile the active owners, reports, completion signals, conflicts, and the single next human gate."
policy:
  allow_implicit_invocation: true
