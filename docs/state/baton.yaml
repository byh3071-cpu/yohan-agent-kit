# 바통 (baton) — 이어받기 객체
# 규약: C:\Users\Public\dev\.agents\SKILL_PIPELINE.md
# ⚠️ 이 파일은 덮어쓰기 대상. 같은 폴더 next-task.md·blockers.md 의 append-only 규칙과 다르다.
# ⚠️ 이어받는 쪽은 실행 전 artifacts 경로를 먼저 읽는다 (바통은 포인터지 사본이 아니다).

goal: 스킬 26개를 잇는 바통 계약 구축 — 단계가 끝날 때 남길 것을 규정해 프롬프트 자동생성·크로스벤더 인계를 연다

stage: 검수

success:
  - "역변환 dry-run: 완성 산출물이 바통으로 빈칸 없이 채워진다 — 통과"
  - "프롬프트 생성 대조: 바통→forge 출력이 손으로 쓴 것보다 나쁘지 않다 — 통과, 결함 1건 적발·수정"
  - "스모크 A(더미 회귀): 판정 6항 전부 YES — 통과 6/6"
  - "스모크 B(실사용): 파이프라인 6단계 실주행, 프롬프트 수기 작성 0회 — 통과. 결함 2건 적발·수정"
  - "벤더 전파 실측: 8개 타겟 전부에 바통 포인터 도달 — 통과"
  - "회귀 무손실: git diff 19 insertions / 0 deletions · git diff --check exit 0 — 통과"
  - "vhk sync --check: 8타겟 일치 + RFC 0062 드리프트 없음 — 통과"
  - "roster 정본 보존: sha256 41d8237… 전후 동일 · brain diff 0 — 통과"
  - "요한 육안 판정: pipeline-usage.md 만 읽고 판단 가능 — 미완 (사람 게이트)"

handshake: 다음 세션이 이 바통 파일 하나만 읽고, 대화 재설명 없이 다음 단계 프롬프트를 자동 생성해 이어간다

non_goals:
  - 신규 스킬 발명
  - 신규 배포 시스템 (vhk sync 8타겟이 이미 함)
  - 신규 프롬프트 엔진 (prompt-forge 재사용)
  - 나머지 스킬 ~7개 주입 (후속)
  - vhk npm 배포 (사람 게이트)
  - aroo 등 타 레포 수정 (크로스레포, 별건)

constraints:
  - "gate: auto 오버라이드 불가 — 스펙·설계 승인 · 머지판정 · publish · 발송 · 삭제 (PAT-003)"
  - "cost 카운터 정의·집계 규칙은 research-loop 확정 표 인용, 재정의 금지"
  - ".agents 는 git 미추적 — 미러 반영은 scripts/sync_layout_to_bootstrap.ps1 → yohan-brain PR, 머지=사람"
  - "AGENTS.md 손수 편집 금지 → RULES.md + vhk sync"
  - "forge 는 세션·벤더·워커 경계에서만 호출 (1회 ≈ 50k 토큰)"
  - "vhk sync 전 설치본이 소스 최신인지 확인 — 구버전이면 최신 규칙 줄이 삭제된다"

artifacts:
  - C:\Users\Public\dev\.agents\SKILL_PIPELINE.md                    # 계약 SoT — 바통 스키마·12단계 표·forge 매핑·비용 규칙
  - C:\Users\Public\dev\.agents\operator\pipeline-usage.md           # 사장님용 사용법 + 레포에 깔기 + 함정 3개
  - C:\Users\Public\dev\.agents\operator\pipeline-smoke.md           # 스모크 A 회귀 테스트 (더미 바통 + 판정 6항)
  - C:\Users\Public\dev\.agents\operator-routing.md                  # 1행 추가
  - C:\Users\Public\dev\.agents\tools_and_skills.md                  # 1c 항목 추가
  - RULES.md                                                         # 포인터 2줄 (코딩 규칙 + 기록 규칙 양쪽)
  - plugins\workflow\skills\research-loop\SKILL.md                   # 계약 주입 ①조사
  - plugins\yohan-core\skills\plan-audit\SKILL.md                    # 계약 주입 ④후 계획감사
  - C:\Users\user\.claude\skills\thought-to-prompt\SKILL.md          # 계약 주입 ②스펙 (글로벌, git 미추적)
  - C:\Users\user\.claude\plans\vhk-indexed-bumblebee.md             # 승인된 플랜
  - C:\Users\user\.claude\plans\vhk-stateless-aho.md                 # VHK 딥리서치 (별개 트랙)

open:
  - "요한 육안 판정 미완 — pipeline-usage.md 읽고 '이렇게 쓰면 되네' 나오는지 (사람 게이트)"
  - "vhk npm 미배포 — 7-16 커밋 4044a7f(AGENTS Roster 포인터) 이후 분이 2.11.0 배포판에 없다. 현재 이 머신만 로컬 빌드 링크(pnpm add -g <vhk경로>)로 우회. **publish = 사람 게이트**"
  - "멀티머신 — 로컬 링크는 이 PC 한정. 노트북에서 vhk sync 돌리면 같은 회귀 재발. 동일 조치 필요"
  - "aroo AGENTS.md Roster 유실 — 같은 원인(구버전 sync). 재sync 필요. 크로스레포라 별건"
  - "vhk `sync --dry-run` 결함 미등록 — 없는 파일을 '· 동일'로 표시. `--check` 는 정상. 이슈 등록은 사람 판단"
  - "나머지 스킬 ~7개 계약 주입 — 스모크 B 통과했으니 진행 가능"
  - "gate: auto 승격 기준 — cost 3회 이상 실측 후. 기준 없이 승격 금지"
  - "⑪지켜보기 단계 스킬 없음 — 신규 스킬 금지 규칙에 걸림. 기존 스킬 커버 여부 먼저 조사"
  - "NotebookLM MCP 미인증 — npx @m4ykeldev/notebooklm-mcp auth (사람). 서드파티 쿠키 기반 감수 판단 필요"
  - "타 레포 전파 — 로컬 링크 후엔 안전. 단 이 머신에서만"

cost:
  elapsed_min:            # 미측정 (빈칸 규칙 — 0 아님)
  수정지시: 1             # 배포층을 .agents 신규 SoT로 설계 → vhk sync 8타겟 재발명이라 폐기
  에러: 4                 # ①artifacts 상세 유실 ②포인터를 기록 규칙에만 넣어 코딩 타깃 6개 미도달 ③vhk sync --dry-run 오표시 ④구버전 vhk sync 가 AGENTS.md Roster 줄 삭제(Codex 적발)
  드리프트: 0
  forge_호출: 3           # ≈50k/회. 이 실측이 「경계에서만 호출」 규칙을 만들었다

next:
  stage: 검수
  skill: cross-check
  vendor: claude
  routing: M

gate: human
